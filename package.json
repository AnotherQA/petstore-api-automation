{
  "name": "api-automation-petstore",
  "version": "1.0.0",
  "description": "Framework de automatización de APIs con Postman y Newman",
  "private": true,
  "scripts": {
    "test": "npm run test:api",
    "test:api": "newman run collections/PetStore_Collection.json -e environments/PetStore_Environment.json -d data/mascotas.csv -r cli,htmlextra --reporter-htmlextra-export reports/index.html"
  },
  "devDependencies": {
    "newman": "^6.0.0",
    "newman-reporter-htmlextra": "^1.22.11"
  }
}
